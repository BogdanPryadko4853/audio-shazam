spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/audio_metadata

  kafka:
    bootstrap-servers: localhost:9092

minio:
  internal-url: http://localhost:9000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true